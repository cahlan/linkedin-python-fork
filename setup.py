# -*- coding: utf-8 -*-
from distutils.core import setup

setup(name = "linkedin-python-fork",
      version = "1.8.2.0",
      description = "Python Interface for the LinkedIn API (includes network updates)",
      author = "Cahlan Sharp",
      license = open("LICENSE").read(),
      long_description = open("README").read(),
      author_email = "cahlan@gmail.com",
      url = "https://github.com/cahlan/linkedin-python-fork/",
      packages = ["linkedin"],
      platforms = ["linux"])
